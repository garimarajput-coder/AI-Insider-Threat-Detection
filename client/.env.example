# URL of the deployed backend API (no trailing slash)
   # Local dev default is http://localhost:5000, so this only needs to be
   # set explicitly in production (e.g. on Vercel).
   VITE_API_URL=https://your-backend.onrender.com
